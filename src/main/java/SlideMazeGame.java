import controller.SlideMazeApplication;
import javafx.application.Application;

/**
 * Contains a main method, that starts the {@link SlideMazeApplication}.
 */
public class SlideMazeGame {
    /**
     * Starts the {@link SlideMazeApplication}.
     */
    public static void main() {
        Application.launch(SlideMazeApplication.class);
    }
}
